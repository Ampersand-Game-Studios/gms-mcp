Turns out our release tweets were getting stuck in X's write path. gms-mcp now posts from a self-hosted runner and falls back to the legacy write endpoint when v2 flakes out. Boring fix, useful result: release posts actually go out again. #GameMaker #gamedev
