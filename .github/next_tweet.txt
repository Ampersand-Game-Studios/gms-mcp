gms-mcp doctor is now a real diagnostics command. Quick checks by default, deeper project/full/client modes when you need them, and `--notify` stays lightweight for startup hooks. Better health checks, safer config inspection, less guesswork. #GameMaker #gamedev
